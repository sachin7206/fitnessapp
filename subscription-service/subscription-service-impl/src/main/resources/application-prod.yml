# Production profile - Cloud deployment
server:
  port: ${PORT:8087}

spring:
  datasource:
    url: ${DATABASE_URL:jdbc:mysql://localhost:3306/fitnessapp_subscriptions?useSSL=true&requireSSL=true&serverTimezone=UTC}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:root}
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false

jwt:
  secret: ${JWT_SECRET}

eureka:
  client:
    enabled: false

logging:
  level:
    root: INFO
    com.fitnessapp: INFO

