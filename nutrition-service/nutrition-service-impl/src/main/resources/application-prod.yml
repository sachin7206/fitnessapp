# Production profile - Cloud deployment
server:
  port: ${PORT:8082}

spring:
  datasource:
    url: ${DATABASE_URL:jdbc:mysql://localhost:3306/fitnessapp_nutrition?useSSL=true&requireSSL=true&serverTimezone=UTC}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:root}
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false

jwt:
  secret: ${JWT_SECRET}

eureka:
  client:
    enabled: false

# Direct service URLs (no Eureka in prod)
services:
  user-service-url: ${USER_SERVICE_URL:http://localhost:8081}
  ai-service-url: ${AI_SERVICE_URL:http://localhost:8086}

logging:
  level:
    root: INFO
    com.fitnessapp: INFO

